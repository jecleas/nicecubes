import { jsxs, jsx } from 'react/jsx-runtime';
import { CheckmarkSolidIcon, CheckmarkIcon } from '@salt-ds/icons';
import { useComponentCssInjection } from '@salt-ds/styles';
import { useWindow } from '@salt-ds/window';
import { clsx } from 'clsx';
import 'react';
import { makePrefixer } from '../utils/makePrefixer.js';
import '../utils/useFloatingUI/useFloatingUI.js';
import '../utils/useId.js';
import '../salt-provider/SaltProvider.js';
import '../viewport/ViewportProvider.js';
import css_248z from './CheckboxIcon.css.js';

const withBaseName = makePrefixer("saltCheckboxIcon");
const CheckboxIcon = ({
  checked = false,
  className,
  disabled,
  error,
  indeterminate,
  validationStatus,
  readOnly
}) => {
  const targetWindow = useWindow();
  useComponentCssInjection({
    testId: "salt-checkbox-icon",
    css: css_248z,
    window: targetWindow
  });
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "aria-hidden": "true",
      className: clsx(
        withBaseName(),
        {
          [withBaseName("checked")]: checked,
          [withBaseName("disabled")]: disabled,
          [withBaseName("error")]: error,
          [withBaseName(validationStatus || "")]: validationStatus,
          [withBaseName("indeterminate")]: indeterminate,
          [withBaseName("readOnly")]: readOnly
        },
        className
      ),
      children: [
        checked && !indeterminate && !readOnly && /* @__PURE__ */ jsx(CheckmarkSolidIcon, { className: withBaseName("icon") }),
        checked && !indeterminate && readOnly && /* @__PURE__ */ jsx(CheckmarkIcon, { className: withBaseName("icon") })
      ]
    }
  );
};

export { CheckboxIcon };
//# sourceMappingURL=CheckboxIcon.js.map
